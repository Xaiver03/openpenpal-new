# Production Environment Configuration
# 生产环境配置 - 使用 PostgreSQL

# Server
PORT=8080
HOST=0.0.0.0
ENVIRONMENT=production

# Database - PostgreSQL for production
DATABASE_TYPE=postgres
DATABASE_NAME=openpenpal

# PostgreSQL connection options (choose one method):

# Method 1: Full connection URL
# DATABASE_URL=postgresql://openpenpal:openpenpal123@localhost:5432/openpenpal

# Method 2: Individual parameters (recommended for security)
DB_HOST=localhost
DB_PORT=5432
DB_USER=openpenpal
DB_PASSWORD=openpenpal123
DB_SSLMODE=disable

# Security (MUST CHANGE IN PRODUCTION!)
JWT_SECRET=KY6QtIecDZocllQSYoqyTkYx8AuKDkpA7RfondzVB2Y=
BCRYPT_COST=12

# Frontend
FRONTEND_URL=https://openpenpal.com

# QR Code
QR_CODE_STORE_PATH=/var/openpenpal/uploads/qrcodes

# Email Configuration
EMAIL_PROVIDER=smtp
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM_ADDRESS=noreply@openpenpal.com
EMAIL_FROM_NAME=OpenPenPal

# AI Configuration (if needed)
# OPENAI_API_KEY=your-openai-key
# CLAUDE_API_KEY=your-claude-key
# AI_PROVIDER=openai