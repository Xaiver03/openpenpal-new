# Frontend Environment Variables - Only NEXT_PUBLIC_ and client-safe variables
# 前端环境变量 - 仅包含NEXT_PUBLIC_前缀和客户端安全的变量

# API Gateway Configuration - 微服务网关配置
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_APP_NAME=OpenPenPal
NEXT_PUBLIC_APP_VERSION=1.0.0
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080

# Microservice URLs - 微服务地址
NEXT_PUBLIC_WRITE_SERVICE_URL=http://localhost:8001
NEXT_PUBLIC_COURIER_SERVICE_URL=http://localhost:8002
NEXT_PUBLIC_ADMIN_SERVICE_URL=http://localhost:8003
NEXT_PUBLIC_OCR_SERVICE_URL=http://localhost:8004

# WebSocket Configuration - WebSocket配置
NEXT_PUBLIC_WS_URL=ws://localhost:8080/api/v1/ws/connect

# Development Settings (Client-safe only)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Feature Flags - AI功能配置
NEXT_PUBLIC_ENABLE_AI_FEATURES=true
NEXT_PUBLIC_ENABLE_MUSEUM=true
NEXT_PUBLIC_ENABLE_ANALYTICS=false

# Environment
NEXT_PUBLIC_ENVIRONMENT=development

# NOTE: ALL SENSITIVE VARIABLES MOVED TO BACKEND
# The following variables have been moved to backend .env files:
# - JWT_SECRET and JWT_REFRESH_SECRET -> backend/.env
# - DATABASE_* credentials -> backend/.env  
# - REDIS_URL -> backend/.env
# - TEST_ACCOUNT_* passwords -> backend/.env
# - CSRF_SECRET -> backend/.env
#
# Frontend should NEVER contain:
# - Database credentials
# - JWT secrets
# - API keys
# - Passwords or sensitive tokens
# - Backend-only configuration